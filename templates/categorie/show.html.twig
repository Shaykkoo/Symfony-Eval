{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    {% include 'base.html.twig' %}

    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ categorie.Titre }}</td>
            </tr>
        </tbody>
    </table>
    
    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">Modifier</a>
    {% endif %}

        {{ include('categorie/_delete_form.html.twig') }}


    <h3>Article de la catégorie {{categorie.titre}} :</h3>

    {% for article in articleCat %}

        <a href ="{{ path('app_article_show', {'id': article.id}) }}">{{ article.titre }}</a><br><br>  

    {% endfor %}

    <a href="{{ path('app_categorie_index') }}">Liste des catégories</a>






{% endblock %}
